{
  "type": "module",
  "scripts": {
    "test": "set NODE_OPTIONS=--experimental-vm-modules && jest --config=jest.config.cjs",
    "mactest": "NODE_OPTIONS=--experimental-vm-modules jest --config=jest.config.cjs"
  },
  "devDependencies": {
    "jest": "^30.2.0",
    "@types/jest": "^30.0.0"
  }
}
